<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ArtApp" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="buttonStyle">@style/NotCapsButton</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="Theme.ArtApp" parent="Base.Theme.ArtApp" />
    <style name="NotCapsButton" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="textAllCaps">false</item>
    </style>
</resources>